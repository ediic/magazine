{% extends 'base.html.twig' %}

{% block title %}Uploads{% endblock %}

{% block body %}
    <h1>Uploads</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ upload.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ upload.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ upload.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ upload.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('uploads_index') }}">back to list</a>

    <a href="{{ path('uploads_edit', {'id': upload.id}) }}">edit</a>

    {{ include('uploads/_delete_form.html.twig') }}
{% endblock %}
